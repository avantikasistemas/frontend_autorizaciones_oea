# Frontend - Autorizaciones Web

Interfaz Vue 3 + Vite para registrar solicitudes de habilitación web y consultar
la vista administrativa cuando el backend devuelve permisos suficientes.

Configura `VITE_API_BASE_URL` en `.env` cuando el backend esté en otro origen.
